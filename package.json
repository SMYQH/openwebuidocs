{
  "name": "openwebuidocs",
  "private": true,
  "scripts": {
    "build": "npm --prefix zh-cn ci && npm --prefix zh-cn run build",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.28.1"
  }
}