---
title: "在 git worktrees 上运行并行代理"
sidebar_position: 14
---

# 在 git worktrees 上运行并行代理

你在一个仓库上有三个独立的任务排队：一个修复不稳定的测试、一个具有 API 影响的依赖升级、以及一个文档更新。一个代理顺序执行浪费了并行性；三个代理在一个工作树中会互相践踏文件。干净的答案是每个任务一个 worktree，每个 worktree 一个工作区，每个工作区一个代理，全部从单个浏览器标签页监督。

**你需要：** 已配置[编码代理](/ecosystem/computer/ai/coding-agents)或 API 模型，以及三个任务确实不共享文件的仓库。原生代理后端独立恢复每个聊天的会话，这使得监督变得合理。

## 操作步骤

1. **创建 worktrees。** 在仓库的工作区中，打开 git 面板中的 worktree 选择器，为每个任务创建一个分支支持的 worktree（`fix/flaky-upload-test`、`chore/bump-sdk`、`docs/cli-reference`）。每个都是磁盘上的单独检出。如果你喜欢，终端等效命令：`git worktree add ../repo-flaky fix/flaky-upload-test`。

2. **每个一个工作区。** 将每个 worktree 目录添加为单独的工作区。工作区限定所有内容（文件、git 面板、终端、聊天），所以三个工作不会污染彼此的差异，每个聊天的文件提及和历史保持按任务划分。

3. **精确简要地指示每个代理。** 在每个工作区中打开一个聊天，选择代理模型，将审批模式设置为 **auto**（读取自由，写入需要门控），并为每个代理提供一个有边界的简要说明以及完成的定义：

   > 修复不稳定的 `test_upload_retry`。先重现它（运行 20 次），识别竞态条件，修复，然后运行 50 次无故障。不要触碰测试和重试模块之外的任何内容。

4. **从一个屏幕监督全部三个。** 分割布局：三个聊天并排，或聊天与 git 面板堆叠。待处理的审批在到达时内联显示在每个聊天中，所以循环是：浏览，点击进入差异，允许或拒绝并附上修正。你在运行中发送的消息会排队等待该代理的下一个轮次。在一个聊天中，代理还可以分派出只读的[子代理](/ecosystem/computer/ai/subagents)进行调查，这样写入路径在每个 worktree 中保持串行，而研究并行化。

5. **像成年人一样顺序落地。** 每个完成时：在该工作区的终端中运行有针对性的测试，在 git 面板中审查完整差异，在分支上提交，然后从主检出中按合理的顺序合并或创建 PR。Worktrees 使合并保持诚实，因为每个分支的树从未被共享。

6. **如果需要，中途离开。** 所有三个任务在服务器端运行，会话按聊天恢复，待处理的审批等待你。从手机上检查就是同样的三个聊天，更小的屏幕。

## 为什么这样有效

隔离是真正的工作所在：worktrees 给每个代理一个私有的文件系统现实，工作区给每个代理一个私有的上下文，所以并行性不会损失正确性。审批模式是按聊天设置的，所以你可以让文档代理以 **full** 模式运行，而依赖升级保持在 **auto**。瓶颈诚实地变成了你的审查带宽；三个是一个舒适的上限，而 git 面板，而不是代理的乐观主义，始终是真相的来源。

**深入了解：** [Git 和 worktrees](/ecosystem/computer/workspace/git) · [工作区](/ecosystem/computer/workspace/workspaces) · [子代理](/ecosystem/computer/ai/subagents) · [审批](/ecosystem/computer/ai/approvals-and-plan-mode)
