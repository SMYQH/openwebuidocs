---
title: "从任何地方监督编码代理"
sidebar_position: 11
---

# 从任何地方监督编码代理

离开家之前，你让 Claude Code 将设置模块迁移出已弃用的 API。午餐时间左右，它到达了你担心的部分：数据库模式。你想看到它提议什么，回答它的疑问，然后让它继续，无需在办公桌前，也无需给它空白支票。

**你需要：** 已连接[编码代理](/ecosystem/computer/ai/coding-agents)（Claude Code、Codex、Cursor、Grok、OpenCode、Cline 或 Pi），通过 [Tailscale](/ecosystem/computer/phone-and-remote/tailscale) 的远程访问，以及主机设置为[保持唤醒](/ecosystem/computer/phone-and-remote/keep-it-running)。

## 操作步骤

1. **离开前开始任务。** 在项目工作区中，在聊天中选择代理的模型，陈述目标和边界，并从 **+** 菜单设置审批模式：

   > 将 `settings/` 从 v1 配置 API 迁移。不要触碰模式或不询问就提交任何内容。

   **auto** 是监督工作的最佳点：读取功能继续，写入和命令等待你。对于不熟悉或高影响的工作，改为在[计划模式](/ecosystem/computer/ai/approvals-and-plan-mode)中开始，先批准计划。

2. **离开。** 任务在服务器端运行。合上笔记本电脑盖是唯一能停止它的事情，这就是为什么保持运行设置很重要。

3. **从手机检查。** 打开同一个聊天。你看到完整的历史：它读取的每个文件、做的每次编辑，以及阻止它的待审批，渲染为带有**允许/拒绝**按钮的工具卡片。

4. **在批准前审查。** 点击工具卡片上的差异，或打开 git 面板查看到目前为止更改的完整情况。如果提议的模式更改不是你想要的，**拒绝**并在同一个聊天中回复：

   > 不要创建迁移。保留旧列，改为编写一个适配器。

   代理保持其上下文并调整方向。当它工作时你发送的消息会排队等待下一个轮次。

5. **在办公桌上落地。** 当它报告完成时，git 面板显示完整的差异。从终端标签页运行测试，然后暂存并提交。会话自动恢复，所以如果有后续工作，同一个对话可以在任何设备上继续。

## 为什么这样有效

代理、差异和你的审批队列都在一个地方，所以"检查代理"是十秒钟的一瞥，而不是 SSH 会话加 `git status`。原生后端使用你现有的订阅和登录；无需购买新内容。而且因为审批是按操作进行的，离开并不意味着放弃控制；只是控制移到了你的口袋里。

**深入了解：** [编码代理](/ecosystem/computer/ai/coding-agents) · [审批和计划模式](/ecosystem/computer/ai/approvals-and-plan-mode) · [聊天功能](/ecosystem/computer/ai/chat-features)
